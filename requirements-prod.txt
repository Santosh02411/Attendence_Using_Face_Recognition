# Production WSGI server. Not in requirements.txt because gunicorn has no
# Windows wheel (it relies on fcntl/os.fork) — Windows deployments should
# use waitress instead (`pip install waitress`, see README "Deploying on
# Windows"). Linux/macOS/Docker deployments: pip install -r requirements.txt -r requirements-prod.txt
gunicorn>=23.0

# Optional error alerting/crash reporting (see error_reporting.py and the
# README's "Error Alerting" section). Only listed here, not in
# requirements.txt, so a local/dev install doesn't pull it in unasked —
# but it's a no-op at runtime everywhere unless SENTRY_DSN is set, so
# there's no harm installing it even if you don't end up using it.
sentry-sdk[flask]>=2.0

