Flask>=2.0
Flask-WTF>=1.2
Flask-Limiter>=3.5
python-dotenv>=1.2.3
# Schema migrations for database/app.db (see migrations/ and db_migrations.py).
# Pulls in SQLAlchemy transitively; nothing in the app uses the ORM, Alembic
# only runs raw SQL via op.execute()/op.create_table() against sqlite.
alembic>=1.13,<2.0
# Upper-bounded below 5.0: OpenCV 5.x removed the Torch/Caffe importers
# (cv2.dnn.readNetFromTorch), which get_embedder() in app.py depends on to
# load models/openface_nn4.small2.v1.t7. An unbounded ">=4.13.0" silently
# resolves to 5.x on a fresh install and breaks face recognition (and the
# embedding tests) at runtime with no code change on our side. Revisit this
# pin if/when the embedder is ported to an ONNX equivalent.
opencv-contrib-python>=4.13.0,<5.0
numpy>=1.24
# Floor raised to the release that closes the known PYSEC advisories
# current as of this writing (heap-overflow/DoS issues in image decoding
# fixed across 12.2.0-12.3.0). Revisit if pip-audit flags a newer one.
Pillow>=12.3.0
# Google OAuth 2.0 / OIDC client for student SSO (see config.py's
# OAUTH_GOOGLE_* settings and app.py's configure_oauth()). Pure-Python
# and lightweight, so it's a normal dependency rather than deferred to
# requirements-prod.txt like sentry-sdk — the SSO feature itself stays
# opt-in via OAUTH_GOOGLE_ENABLED regardless.
Authlib>=1.3
# Used by notifications.py to send SMS via Twilio's plain HTTPS REST API
# (no twilio SDK dependency needed for that). Also a transitive
# dependency of several packages already, but listed explicitly since
# notifications.py imports it directly.
requests>=2.31
